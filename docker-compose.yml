# 一鍵啟動 ToolHub(前端 + AI 後端)
#   1. 複製 .env.example 成 .env,填入你的 ANTHROPIC_API_KEY
#   2. docker compose up --build
#   3. 開 http://localhost:8080
services:
  toolhub:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
    restart: unless-stopped
